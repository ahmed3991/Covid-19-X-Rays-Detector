runtime: python37
entrypoint: gunicorn -b :$PORT main:app

runtime_config:
    python_version:3
# [START handlers]
handlers:
- url: /static
  static_dir: static
- url: /.*
  script: auto
# [END handlers]